version: '3.8'

services:
#   nginx:
#     image: nginx:1.21
#     volumes:
#       - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
#     ports:
#       - 80:80
#     depends_on:
#       - backend

  backend:
    build:
      context: henrypi-api
      dockerfile: Dockerfile
    image: hleong25/henrypi-api
    ports:
      - 8000:8000
      - 9000:9000
      - 9001:9001
    privileged: true
    # depends_on:
